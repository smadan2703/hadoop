---

#- name : "Download Java JDK RPM for Hadoop & Hbase"
#  get_url:
#    url: {{ java_version }}
#    dest: /tmp/java.rpm
#    mode: 0755

- name: "Check Java is installed or not"
  shell: command -v java
  register: javaout
  ignore_errors: True

- name: "Install Java"
  yum:
    name: {{ java_version }}
    state: present
  when: javaout > 0